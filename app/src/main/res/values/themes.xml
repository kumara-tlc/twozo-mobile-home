<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TLCCRMAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TLCCRMAndroid" parent="Base.Theme.TLCCRMAndroid" >
        <item name="colorPrimary">@color/theme_light_primary</item>
        <item name="colorSecondary">@color/theme_dark_background</item>
        <item name="colorSecondaryContainer">@color/theme_light_tertiary</item>
        <item name="colorOnPrimaryContainer">@color/theme_dark_background</item>
        <item name="android:fontFamily">@font/lexend</item>
        <item name="android:statusBarColor">@color/theme_light_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>